# Domain Docs

This repository uses a single domain context.

- Glossary: read `CONTEXT.md` at the repository root.
- Architectural decisions: read relevant files under `docs/adr/`.
- Keep `CONTEXT.md` as a concise domain glossary, not an implementation specification.
- Use the glossary's canonical terms in issues, designs, and code discussions.
